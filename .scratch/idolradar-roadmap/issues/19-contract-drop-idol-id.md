# 19 — contract：删除用户表上的单值 idol 字段及其索引

**What to build:** 移除已无任何读取方的旧字段与其上的索引，守护关联表成为唯一事实来源。

这是 expand-contract 的收尾。执行前必须确认没有任何代码路径仍在读取旧字段——包括测试代码，它们此前在 expand 阶段被刻意保留未动。

**Blocked by:** 17 — migrate：Worker 与推送侧读路径切换到守护关联表；18 — migrate：API 侧读路径切换到守护关联表

**Status:** ready-for-agent

- [ ] 确认全代码库无任何位置读取旧字段
- [ ] 停止双写，移除旧字段与其上的四个索引
- [ ] 相关测试改为面向守护关联表
- [ ] 迁移文件写明该变更不可逆，以及回滚需要的前置条件
- [ ] 迁移在生产数据规模下的执行时间与锁行为已评估
