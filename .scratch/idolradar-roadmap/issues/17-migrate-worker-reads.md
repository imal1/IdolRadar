# 17 — migrate：Worker 与推送侧读路径切换到守护关联表

**What to build:** 抓取轮次结束后筛选推送目标用户的逻辑改为从守护关联表读取，行为保持不变。

由于此时每个用户仍然只有一条守护关系（小程序端限制），切换后的推送行为与切换前完全等价，PRD 中「同轮每用户最多一条推送」的规则一个字都不用改。

**Blocked by:** 16 — expand：新增守护关联表，与既有单值字段并存双写

**Status:** ready-for-agent

- [ ] 推送目标筛选改为从守护关联表读取
- [ ] 筛选查询走索引，不引入全表扫描
- [ ] 单守护场景下推送行为与切换前完全一致
- [ ] 用户守护多位 idol 时的推送分配行为明确并有测试覆盖，即使当前客户端不会产生该场景
- [ ] 旧字段在本阶段仍被写入，不被读取
