# 18 — migrate：API 侧读路径切换到守护关联表

**What to build:** 雷达首页、动态流、用户引导态等所有面向客户端的读接口改为从守护关联表读取当前守护关系。

同时，更换守护对象的接口语义从「设置用户的 idol 字段」变为「替换该用户唯一的守护关系」。小程序端三个页面无需任何改动，因为对外行为完全等价。

**Blocked by:** 16 — expand：新增守护关联表，与既有单值字段并存双写

**Status:** ready-for-agent

- [ ] 首页、动态流、引导态接口全部改为从守护关联表读取
- [ ] 更换守护对象的接口改为替换唯一守护关系，返回结构不变
- [ ] 小程序端不做改动即可正常工作
- [ ] 无守护对象时的引导行为不变
- [ ] 旧字段在本阶段仍被写入，不被读取
- [ ] 既有 API 契约测试全部通过，未修改断言
