---
status: accepted
---

# 更换守护对象时保留旧动态数据

用户更换守护对象时，仅替换用户的守护关系；旧 idol 的动态数据继续保留。雷达首页和订阅推送始终按用户当前守护的 idol 过滤，因此旧动态对该用户不再展示，旧 idol 的新动态也不再向该用户推送。

不采用物理删除：动态属于 idol 的共享数据，并非某个用户私有；删除不可逆，会让用户换回原 idol 后无法恢复历史内容，也会破坏原文链接唯一索引承担的全局去重语义。选 idol 页的“旧动态会清空”描述的是用户可见列表被切换，并不代表删除数据库中的动态记录。
